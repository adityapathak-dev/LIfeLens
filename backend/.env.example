# Choose ONE provider
# Choose ONE provider
LLM_PROVIDER=openai
# anthropic | openai | groq

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Groq: console.groq.com — no card required, generous free tier (no own models;
# runs open-weight models like Llama on fast custom hardware).
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

PORT=4000
